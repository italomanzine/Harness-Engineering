# Plan: Demo Feature

## Technical Summary

Usar o sensor `harness` para validar que o próprio harness está íntegro.

## TDD Strategy

- O teste operacional é `rtk bash scripts/evaluate.sh specs/demo-feature`.
- O sensor executado é `rtk bash scripts/validate.sh`.
